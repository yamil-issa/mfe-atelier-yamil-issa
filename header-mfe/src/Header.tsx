import React from 'react';

const Header = () => {
  return <header style={{ backgroundColor: '#333', color: '#fff', padding: '10px', textAlign: 'center' }}>Header</header>;
};

export default Header;